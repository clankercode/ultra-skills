#!/usr/bin/env bash
set -euo pipefail
scripts=(install-symlinks-claude.py install-symlinks-codex.py install-symlinks-opencode.py)
for script in "${scripts[@]}"; do
  echo "=== Running $script ==="
  python3 "$script"
done
echo "=== All done ==="
